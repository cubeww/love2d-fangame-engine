Object.extends('SlideBlockL', 'SlideBlock', function(self)
    self.visible = true
    self.sprite = Sprites.sSlideBlockL
    self.mask = Same
    self.persistent = false
    self.depth = 100

    self.h = -2
end)